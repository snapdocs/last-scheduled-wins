# frozen_string_literal: true

source 'https://rubygems.org'

# Harness for running the spec suite. LastScheduledWins itself is a single
# file meant to be copied into a host application, not installed as a gem.
gem 'activesupport', '~> 8.0'
gem 'connection_pool', '~> 2.5'
gem 'redis', '~> 5.1'
gem 'rspec', '~> 3.13'
gem 'sidekiq', '~> 8.0'
gem 'timecop', '~> 0.9'
